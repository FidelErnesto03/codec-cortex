# Agent handoff

Read `CODEC_CORTEX_LEARNING_ENGINE_SPEC.md` first. Treat it as the contract.

Your final deliverable must be:

```text
codec-cortex-learning-engine-<version>.tar.gz
```

It must include implementation, tests, install script, regression script, `PATCH_SUMMARY.md` and `CERTIFICATION_REPORT.md`.

Do not declare success unless the package is generated and certification is PASS.
