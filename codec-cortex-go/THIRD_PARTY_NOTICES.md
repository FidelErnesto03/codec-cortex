# Third-party notices

This project vendors the following package so builds and tests work offline:

- `golang.org/x/text/unicode/norm`
- `golang.org/x/text/transform`

The vendored source and its BSD-style license are under `vendor/golang.org/x/text/`.
The files were taken from the Go 1.23 distribution's vendored copy.
