[package]
name = "cortex-parser"
version = "0.1.0"
edition = "2021"
description = "CORTEX 0.1 parser — implementation from normative artifacts"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[[bin]]
name = "cortex-parse"
path = "src/main.rs"
