# CI workflow for codec-cortex CLI
# Runs on every PR and push to main

name: CI

on:
  push:
    branches: [main]
    tags: ["v*"]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      publish:
        description: "Publish to PyPI"
        type: boolean
        default: false

jobs:
  test:
    name: "Python ${{ matrix.python-version }}"
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.9", "3.10", "3.11", "3.12"]
    defaults:
      run:
        working-directory: cli

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0  # needed for setuptools-scm

      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .[dev]

      - name: Lint with ruff
        run: pip install ruff && ruff check src/

      - name: Test with pytest and coverage gate
        run: |
          python -m pytest src/tests/ -q --cov-config ../.coveragerc

      - name: Verify documentation API sources
        run: |
          for f in ../docs/reference/api/*.cortex; do cortex verify "$f" --strict; done
          cortex docstring canonicalize > /dev/null

      - name: Verify canonical artifacts
        run: |
          cortex verify-view ../skill/cortex/SKILL.md
          cortex roundtrip-bidir ../skill/cortex/SKILL.md || echo "⚠️ roundtrip-bidir non-idempotent for v2 format (known limitation)"
          cortex verify --strict ../skill/cortex/SKILL.md
          cortex inspect ../skill/cortex/SKILL.md --format json > /dev/null

      - name: Benchmark inventory gate
        run: |
          cortex benchmark --list

  build:
    name: Build package
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cli

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build wheel
        run: |
          pip install build
          if [[ "${GITHUB_REF}" == refs/tags/v* ]]; then
            export SETUPTOOLS_SCM_PRETEND_VERSION="${GITHUB_REF_NAME#v}"
          fi
          python -m build --wheel

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: cli/dist/

  publish:
    name: Publish to PyPI
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [test, build]
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: cli

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build wheel
        run: |
          pip install build
          export SETUPTOOLS_SCM_PRETEND_VERSION="${GITHUB_REF_NAME#v}"
          python -m build --wheel

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: ${{ secrets.PYPI_API_TOKEN }}
          packages-dir: cli/dist/
          attestations: false
          skip-existing: true
