Perfil: CORTEX-FULL
<!-- cortex-render: manual audit; roundtrip: false -->

# HCORTEX-AUDIT — Verificación ejecutable de codec-cortex v1.1.5

## $1 · IDENTITY

IDN:audit{name:"codec-cortex-v1.1.5-executable-verification", date:"2026-06-26", auditor:"ChatGPT", mode:"executable+adversarial"}
DOM:scope{project:"codec-cortex", artifact:"codec-cortex-1.1.5.tar.gz", spec_reference:"SKILL.md / SKILL_canon.md", objective:"verify claimed fixes and identify residual gaps"}

## $2 · EXECUTIVE RESULT

CLAIM:package_execution{statement:"The v1.1.5 tarball installs and executes successfully in an isolated virtual environment", evidence:"cortex --version => cortex 1.1.5", status:"measured"}
CLAIM:test_suite{statement:"The official test suite passes", evidence:"python -m pytest src/tests/ -q => 169 passed in 5.83s", status:"measured"}
CLAIM:demo{statement:"The v1.1.5 demo passes", evidence:"bash scripts/cortex_demo_v1_1_5.sh => ALL v1.1.5 FIXES VERIFIED; exit 0", status:"measured"}

AUD:acceptance{event:"v1.1.5 verification", evidence:"installation, official tests, official demo and adversarial probes executed", result:"acceptance with conditions", date:"2026-06-26"}

## $3 · VERIFIED FIXES

AUD:fix_e033{event:"$0 operational memory rejection", evidence:"verify bad brain with FCS/OBJ in $0 returns E033 + E024", result:"verified", date:"2026-06-26"}
AUD:fix_fcs_obj_outside_zero{event:"FCS/OBJ in $0 do not satisfy brain invariant", evidence:"brain with only FCS/OBJ under $0 fails as expected", result:"verified", date:"2026-06-26"}
AUD:fix_recover_entry_first{event:"entry-first recovery visibility", evidence:"recover legacy entry-first file creates $1: RECOVERED CONTENT; verify passes; render audit shows IDN and KNW", result:"verified", date:"2026-06-26"}
AUD:fix_add_zero{event:"add --section $0 rejects operational entry", evidence:"cortex add --section 0 --sigil NXT returns E033 and original file remains valid", result:"verified", date:"2026-06-26"}
AUD:fix_non_bypassable{event:"non-bypassable governance invariants", evidence:"delete FCS:primary with --force and --no-validate-write returns rc=1 and file hash remains unchanged", result:"verified", date:"2026-06-26"}

## $4 · RESIDUAL BREACHES

RSK:empty_required_values{risk:"Critical required fields can be present but empty", impact:"A brain can pass verify --strict with active FCS/OBJ that contain empty what/goal/success values; this weakens the SKILL requirement that Level 2 contain meaningful focus and objective before acting", mitigation:"Add non-empty validation for required fields of critical sigils; treat blank strings and whitespace-only strings as missing for FCS/OBJ/STP/CNST/RSK/AUD/CLAIM/LIM/KNW/SES/LNG", status:"open", severity:"high", survive:"recovery"}

AUD:empty_required_values_reproduction{event:"adversarial empty-field test", evidence:"FCS:primary{what:\"\", priority:\"high\", status:\"current\", survive:\"min\"} and OBJ:main{goal:\"\", status:\"current\", success:\"\", survive:\"min\"} pass verify --strict --kind brain with errors:0", result:"breach confirmed", date:"2026-06-26"}

RSK:recover_existing_zero_hidden_entries{risk:"recover returns success while preserving an already existing $0 that contains operational entries", impact:"The recovered output remains invalid under verify --strict with E033/E024, yet recover exits rc=0 and reports no diagnostics; this can mislead an operator into thinking the file was repaired", mitigation:"When recover sees operational entries inside an existing $0, move them to $1: RECOVERED CONTENT, emit AUD/RSK, and return non-zero if it cannot produce a conformant artifact", status:"open", severity:"medium", survive:"work"}

AUD:recover_existing_zero_reproduction{event:"recover existing $0 with hidden operational entries", evidence:"input had IDN/FCS/OBJ below $0; recover rc=0; recovered file still fails verify --strict with 3x E033 + 2x E024", result:"breach confirmed", date:"2026-06-26"}

LIM:external_skill_mismatch{limit:"The tarball carries SKILL_canon.md v1.2.0, but the externally uploaded SKILL.md in this conversation declares v1.1.0", scope:"audit traceability", status:"open"}

## $5 · POSITIVE CONTROLS

AUD:comment_first_recovery{event:"legacy file with preamble comment", evidence:"recover strips one preamble line, reconstructs $0, creates $1: RECOVERED CONTENT, verify passes, HCORTEX shows recovered IDN/KNW", result:"verified", date:"2026-06-26"}
AUD:move_to_zero_blocked{event:"move operational entry to $0", evidence:"move FCS:primary --to-section $0 --force returns E015 with E033/E024 diagnostics; source file remains valid", result:"verified", date:"2026-06-26"}
AUD:invalid_status_priority{event:"invalid status/priority", evidence:"invalid priority/status are caught in strict validation", result:"verified", date:"2026-06-26"}

## $6 · RECOMMENDATION

STP:v1_1_6{action:"Produce v1.1.6 focused only on residual semantic hardening", reason:"v1.1.5 closes the structural $0 invisibility breach, but still accepts empty critical semantics and recover can report success while output remains invalid", owner:"implementing agent", status:"recommended", survive:"min"}

NXT:required_non_empty{action:"Add E034_CRITICAL_REQUIRED_FIELD_EMPTY as non-bypassable for critical sigils", trigger:"before declaring full conformance", status:"recommended", survive:"recovery"}
NXT:recover_zero_repair{action:"Teach recover to migrate operational entries out of existing $0 or fail explicitly", trigger:"before claiming recovery handles non-conforming artifacts", status:"recommended", survive:"work"}
NXT:benchmark_cases{action:"Add benchmark tests for empty required fields and recovery of existing $0 with hidden entries", trigger:"before next release report", status:"recommended", survive:"work"}

## $7 · FINAL JUDGMENT

CLAIM:judgment{statement:"v1.1.5 is materially stronger than v1.1.4 and closes the memory-invisible $0 breach, but it should remain acceptance-with-conditions rather than full acceptance", evidence:"169 tests and official demo pass; adversarial tests found empty critical fields accepted and recovery success with invalid output in an existing-$0 case", status:"measured"}
